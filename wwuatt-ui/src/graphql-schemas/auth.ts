import gql from 'graphql-tag';

// export const LOGIN = gql`mutation login($token: String!){
//   login(token: $token) { }
// }`;

// export const LOGOUT = gql`mutation logout($token: String!){
//   logout(token: $token) {}
// }`;
